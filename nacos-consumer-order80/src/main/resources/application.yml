server:
  port: 80

spring:
  application:
    name: order-consumer
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

service-url:
  nacos-user-service: http://payment-provider